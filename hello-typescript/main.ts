// Criação de variavel com js puro
var minhaVar = "minha variavel";

// JavaScript puro
function minhaFunc(x, y) {
  return x + y;
}

// Podemos usar aqui qualquer coisa de javascript puro
// Ecma Script 2015

let num = 2;
const PI = 3.14;

var numero = [1, 2, 3];
numero.map((e) => e * 2); //ES 2015

class Matematica {
  soma(x, y) {
    return x + y;
  }
}

// JavaScript puro
var n1 = "sasasas0";
n1 = 4;

// TypeScript conseguimos tipar as variaveis
var n1: string = "sasassa";
